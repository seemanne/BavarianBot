version: '0.1.0'

services:
  maggus:
    image: maggus:latest
    env_file: .dev.env
    restart: always
    ports:
      - "8000:80"
    volumes:
      - ./volumes/db:/db:Z

networks:
  default:
    name: maggnet